{
  "name": "action-secret-replace",
  "version": "1.0.0",
  "description": "A github action for replacing a file in source with content defined in a Github Secret. Useful for replacing a template config file with real values when deploying into an environment.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Radioh/action-secret-replace.git"
  },
  "keywords": [
    "github",
    "actions",
    "secret",
    "replace"
  ],
  "author": "Jeppe Ærenlund",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Radioh/action-secret-replace/issues"
  },
  "homepage": "https://github.com/Radioh/action-secret-replace#readme",
  "dependencies": {
    "@actions/core": "^1.2.6"
  }
}
